<ul class="list-inline product-controls hidden-sm hidden-xs">
  <li><%= link_to icon('check'), worker_booking_path(current_user.id, booking.id, status: "accepted"), method: :patch, remote: true %></li>
  <li><%= link_to icon('ban'), worker_booking_path(current_user.id, booking.id, status: "rejected"), method: :patch, remote: true %></li>
</ul>
